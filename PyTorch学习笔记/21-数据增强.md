# 数据增强

数据增强是在不增加实际数据量的情况下，通过对训练图片进行各种变换来扩充数据集的一种方法，可以有效提高模型的泛化能力，防止过拟合。

常见的数据增强方式包括几何变换和颜色变换。几何变换有随机裁剪、水平翻转、旋转、缩放等操作，颜色变换有调整亮度、对比度、饱和度等。

数据增强只在训练时使用，验证和测试时使用原始图片或确定性的预处理，因为评估时需要得到稳定的结果。

在PyTorch中，可以使用torchvision.transforms模块来实现各种数据增强操作，通过Compose将多个变换组合在一起。
